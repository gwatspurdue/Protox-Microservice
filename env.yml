# Protox Microservice Environment Configuration

# Protox API Configuration
PROTOX_BASE_URL: https://protox.org/api
PROTOX_TIMEOUT: 30

# FastAPI Server Configuration
HOST: 0.0.0.0
PORT: 8000
WORKERS: 4

# Logging Configuration
LOG_LEVEL: INFO

# Cache Configuration
ENABLE_CACHE: true
MAX_CACHE_SIZE: 1000
